console.log("#bus-status-<%=@bus.id%>")
var button = $("#bus-status-<%=@bus.id%>");
button.text('<%= j @bus.approved? ? "Disapprove" : "Approve" %>');
var flashContainer = $(".flash");
console.log(flashContainer)
flashContainer.empty(); 
$(".flash").remove();
var content_box = $(".content")
content_box.prepend('<%= j render partial: "layouts/flash" %>');